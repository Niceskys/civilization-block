# 2026-07-05 第一批候选文件内容预读与冲突风险建议

> 建议状态：待 Codex / 用户审核后回写。  
> 本文件仅为 `ChatGPT_修改暂存区/` 内的建议文件，不属于《文明块》正式规则，不得直接视为已生效内容。  
> 本文件用于记录第一批 MVP 冻结候选正式文件的内容预读结果、插入风险与拆批建议。  
> 本文件不修改正式文档，不修改 Runtime / C# / Unity 工程，不创建 PR。

---

## 本轮目标

待 Codex / 用户审核后回写。

本轮目标是在已经完成第一批候选文件路径核验后，继续预读部分高优先级正式文件内容，判断后续是否适合直接回写 MVP 冻结内容，是否存在已有规则冲突、章节过载、范围膨胀或插入位置不明确等风险。

本轮只做内容预读和风险记录，不进入正式回写。

---

## 本轮预读文件

待 Codex / 用户审核后回写。

本轮已预读以下高优先级候选文件：

1. `文明块/00 - 项目总纲/00.27 前30秒与首次城市响应规则_v1.md`
2. `文明块/04 - 资源与生产链/4.2 生产链路总览.md`
3. `文明块/02 - 建筑系统/2.1 建筑通用规则.md`
4. `文明块/03-NPC 系统/3.3 工作分配逻辑.md`
5. `文明块/05-UI 与交互设计/5.1 全局界面.md`
6. `文明块/05-UI 与交互设计/5.2 建造系统 UI.md`
7. `文明块/05-UI 与交互设计/5.3 建筑详情 UI.md`
8. `文明块/04 - 资源与生产链/4.1 全资源清单.md`

四个基础建筑详情文件已确认路径存在，但本轮尚未逐项预读正文结构。

---

## 总体判断

待 Codex / 用户审核后回写。

本轮内容预读后，第一批正式回写不应一次性改完 13 个候选文件。

原因：

1. 多个正式文件已有大量中后期内容，不能用 MVP 冻结内容直接覆盖。
2. `4.2 生产链路总览.md` 已包含熔炉、铁矿石、燃料、垃圾处理、科技、防御等中后期链路，因此只能新增“MVP 子集 / MVP 停止线”，不能删改全量链路。
3. `4.1 全资源清单.md` 已是 16 种全量资源表，并含稳定资源 ID，不能改成只剩 MVP 资源；只能新增“MVP 主循环资源子集”。
4. `3.3 工作分配逻辑.md` 已含岗位权威边界和大量职业 / 动画 / 死亡 / 夜班 / 属性内容，第一批只能新增 MVP 劳动力边界，不能重写工作系统。
5. `5.1 全局界面.md` 已含商店、任务、科技、贸易、事件日志、数据仪表盘等完整 UI 入口，第一批只能新增“MVP 首屏 / 前30秒 UI 子集”，不能删除全量 UI。
6. `5.2 建造系统 UI.md` 已有五个一级 Tab 和完整建造面板分类，第一批只能新增“MVP 初始可见建筑卡 / 前30秒限制”，不能把全量建造 UI 改成四张卡永久结构。
7. `5.3 建筑详情 UI.md` 已包含建筑内部视图、17 种建筑、多分区交互和复杂效率拆解，第一批只能新增“MVP 建筑详情最小显示模式”，不能覆盖完整建筑详情方案。
8. `00.27 前30秒与首次城市响应规则_v1.md` 与 MVP 冻结方向高度一致，是第一批最适合直接小改的文件。

因此，建议第一批正式回写拆成至少两步：

- 第一小批：`00.27`、`4.2`、`4.1`，先冻结 MVP 总体验与基础闭环子集。
- 第二小批：`2.1`、`3.3`、`5.1`、`5.2`、`5.3`，再补建筑、NPC、UI 的 MVP 子集边界。
- 四个基础建筑详情文件作为第三小批单独预读后再处理。

---

## 单文件预读结果

待 Codex / 用户审核后回写。

### 1. `00.27 前30秒与首次城市响应规则_v1.md`

#### 现有状态

该文件已经相当接近 MVP 冻结方向。

已有内容包括：

- 前30秒目标是让玩家理解城市会生长、可以马上做第一件事、城市会响应操作。
- 新存档默认暂停或未推进。
- 初始 2×2 地皮已可建造。
- 第一次合法确认建筑放置并创建施工任务后，世界进入正常时间推进。
- 前30秒只允许资源栏、地皮可建状态、NPC 存在感、快捷键提示、一个非强制推荐行动。
- 禁止复杂面板、任务列表、生产链详情、敌袭说明、商店促销、签到式提示等。
- 建造面板中优先推荐房屋，但农田、水井、树场等基础建筑仍可选择。
- 确认第一座建筑后要求施工虚影、NPC 施工表现、资源栏扣除或锁定、时间开始流动提示。
- 第一座建筑完成后要求至少两类反馈，房屋应触发入住反馈。

#### 风险判断

风险较低。

该文件不需要大改，建议只新增或微调：

- 明确“前30秒属于 MVP 首轮体验的一部分”。
- 明确前30秒不展示联机 / 模组入口。
- 明确任务、商店、地下、工具只作为后续系统，不进入首轮体验。

#### 建议插入位置

建议在 `## 一、核心目标` 后追加一个小节：

```md
### MVP 首轮体验边界
```

或在 `## 三、前30秒信息层级` 中补充两条禁止项。

#### 禁止做法

不得重写现有前30秒规则，不得删除已有首建加速、堆叠提示和真实反馈边界。

---

### 2. `4.2 生产链路总览.md`

#### 现有状态

该文件内容范围很大，已包含：

- 树场 → 木材。
- 采掘场 → 铁矿石 + 石头。
- 熔炉房：铁矿石 + 燃料 → 铁锭。
- 农田 → 食物。
- 水井 → 水。
- 垃圾处理场多模式。
- 研究站、疗愈所、工坊、管道、哨塔、城墙、陷阱。
- 燃料生产链。
- 熔炉房冷启动链路。
- 通用生产批次与连续生产。
- 存档与读档规则。
- Runtime 最小实现状态。

#### 风险判断

风险高。

如果直接写“基础城市闭环”，容易与现有全量生产链混在一起，造成误解：玩家或 Codex 可能以为熔炉、燃料、垃圾、科技、防御也应进入 MVP。

#### 建议处理方式

不要删除现有全量链路。

应在文件开头 `## 生产链路` 之前或之后新增一个明确小节：

```md
## MVP 主循环子集
```

写明：

- MVP 只启用木材、食物、水、人口、空闲工人相关闭环。
- MVP 基础城市闭环为：木材驱动建造 → 房屋提供居住容量 → 水井提供水 → 农田消耗水并产食物 → 人口聚合消耗食物与水 → 空闲 NPC 自动补位 → 树场提供可再生木材。
- 该小节是“首版可玩子集”，不是删除全量生产链。
- 熔炉、铁矿石、铁锭、燃料、垃圾处理、科技、防御、管道等仍为后续正式链路，不进入 MVP 主循环。

#### 禁止做法

- 不得删除熔炉房冷启动链路。
- 不得删除 Runtime 最小实现状态。
- 不得把全量生产链改名为 MVP 生产链。
- 不得把铁矿石、燃料、垃圾处理、科技、防御写成 MVP 必做。

---

### 3. `4.1 全资源清单.md`

#### 现有状态

该文件是全资源清单，共 16 种资源，且包含稳定资源 ID。

已有资源包括：

- 食物、水、木材、石头、垃圾。
- 铁矿石、铜矿石、水晶矿、铁锭、铜锭。
- 水晶碎片、晶能宝石。
- 星辉、肥料、燃料、科技点。
- Runtime 稳定资源 ID 表。

#### 风险判断

风险高。

不能为了 MVP 把全资源清单缩减为 5 个资源，否则会破坏正式资源定义、稳定 ID、未来存档 / 模组 / 联机引用契约。

#### 建议处理方式

在 `# 全资源清单（共16种）` 后追加小节：

```md
## MVP 主循环资源子集
```

写明：

- MVP 主循环只要求木材、食物、水、人口、空闲工人进入核心 UI 和运行闭环。
- 其中人口与空闲工人更偏向城市状态 / 劳动力状态，不一定是库存资源。
- 其余正式资源保留在全资源清单中，但不进入 MVP 主循环。
- 稳定资源 ID 不得因为 MVP 裁剪而删除或重命名。

#### 禁止做法

- 不得删除 16 种资源表。
- 不得删除 Runtime 稳定资源 ID。
- 不得把星辉、燃料、铁锭、科技点直接写入 MVP 主循环。

---

### 4. `2.1 建筑通用规则.md`

#### 现有状态

该文件已经是已定稿状态，包含：

- 建造资源消耗。
- 施工时长。
- 工人参与施工。
- 首次合法放置的时间启动触发。
- 新存档首次建造加速。
- 首建加速状态、预约、消耗、取消、失败、暂停、并行施工、事件顺序、存档要求。
- 耐久、维修、拆除、升级等规则。

#### 风险判断

中高风险。

该文件已经有大量严格规则，不能在第一批中大段插入新的 MVP 建筑流程，避免与已有首建加速、存档要求和拆除规则重复。

#### 建议处理方式

只新增一个短小节：

```md
### MVP 基础建筑范围
```

建议放在 `### 建造规则` 后、`#### 首次合法放置的时间启动触发` 前。

内容只写：

- MVP 主循环只要求房屋、农田、水井、树场四类建筑。
- 其他建筑可存在于完整设计中，但不进入 MVP 主循环。
- MVP 建造流程继续遵守本文件既有放置、施工、首次启动、首建加速、存档要求。
- 小草、碎石、贴图颗粒等视觉细节不得单独作为阻挡，详细数据模型后续由地形与建筑模型边界回写维护。

#### 禁止做法

- 不得重写首建加速规则。
- 不得改动存档要求。
- 不得新增熔炉、矿场、商店、地下入口为 MVP 必建建筑。
- 不得在第一批中展开完整地形 / 地基 / 承重数据模型。

---

### 5. `3.3 工作分配逻辑.md`

#### 现有状态

该文件已是已定稿状态，并且内容复杂。

已有内容包括：

- 权威边界。
- Runtime 最小岗位契约。
- UI 只能提交命令并消费事件。
- 当前切片尚未实现职业硬条件、属性适配、班次、到岗寻路和生产贡献。
- 工作分配方式。
- 硬性分配条件。
- 分配与重新分配事务。
- 自动分配边界。
- 空闲 NPC 低效杂活。
- 夜班机制。
- 属性与工作适配。
- NPC 死亡规则。
- 多职业多阶段工作循环。

#### 风险判断

高风险。

该文件已经同时存在“最小岗位契约”和大量中后期职业 / 动画 / 夜班 / 死亡 / 恐惧内容。第一批回写必须明确是“给 MVP 添加启用子集”，不是重写 NPC 系统。

#### 建议处理方式

在 `### Runtime最小岗位契约` 后追加：

```md
### MVP 劳动力启用子集
```

内容只写：

- MVP 真实保留人口数、空闲 / 占用工人、施工参与、基础生产岗位占用、房屋入住状态、城市级食物 / 水消耗、等待工人原因。
- MVP 不启用职业成长、情绪、恐惧、社交、好感度、个体记忆、完整路径驱动生产结算、个体吃饭喝水行为、家庭关系。
- 表现层移动和动画可以存在，但岗位占用才是权威状态。

#### 冲突点

现有 `自动分配边界` 写明系统不得自动替玩家确认首次分配、自动换岗或自动解除原岗位；而 MVP 建议中有“空闲 NPC 自动补位到生产 / 施工岗位”。这两者存在潜在冲突。

建议处理为：

- “施工参与 / 低效杂活 / MVP 自动补位”必须定义为 MVP 临时劳动力策略或空闲杂活，不等于正式岗位自动分配。
- 正式岗位分配仍遵守玩家确认和 `WorkerAssignmentSystem` 权威边界。
- 若未来要启用真正自动分配，必须另开规则裁决，不得在第一批中直接改写自动分配边界。

#### 禁止做法

- 不得删除自动分配边界。
- 不得删除 Runtime 最小岗位契约。
- 不得把职业、夜班、恐惧、死亡、工作循环细节写成 MVP 必做。

---

### 6. `5.1 全局界面.md`

#### 现有状态

该文件是全局 UI 文件，已有内容包括：

- 极简悬浮式布局。
- 主菜单。
- 顶部资源栏默认显示食物、水、木材、石头、星辉。
- 功能按钮：商店、任务、NPC 列表、科技、挑战、贸易路线、事件日志、设置、保存。
- 时间控制状态显示。
- 悬浮面板。
- 城市脉搏条。
- 数据仪表盘。
- 事件日志。

#### 风险判断

中高风险。

该文件包含完整游戏 UI，不适合直接改成 MVP 只显示几个按钮。第一批应该增加“前30秒 / MVP 首屏启用子集”，而不是删除完整功能按钮。

#### 建议处理方式

在 `## 游戏主界面元素` 后添加：

```md
### MVP 首屏启用子集
```

内容写明：

- MVP 前30秒默认只启用资源栏、时间状态、建造入口、轻量提示、少量 NPC 存在感。
- 商店、任务、科技、挑战、贸易路线、事件日志、数据仪表盘等入口属于完整 UI，可存在于长期设计中，但不得在前30秒自动打开或强制引导。
- MVP 资源栏应至少显示木材、食物、水、人口、空闲工人；与全资源栏的完整配置并存。

#### 禁止做法

- 不得删除商店、任务、科技等长期 UI 入口。
- 不得把完整快捷功能区改成永久只有 MVP 按钮。
- 不得让 UI 自行计算 Runtime 诊断。

---

### 7. `5.2 建造系统 UI.md`

#### 现有状态

该文件已有完整建造系统 UI，包括：

- 建造面板入口。
- 五个一级 Tab：常用、生存、生产、设施、防御。
- 建筑卡显示层级。
- 搜索与二级筛选。
- 当前任务推荐。
- 免费重新放置功能。
- 施工加速状态与互斥显示。
- 建造面板分类教程节点。
- 存档与异常恢复。
- 升级交互。

#### 风险判断

中高风险。

不能把该文件改成只有四张建筑卡，否则会破坏完整建造 UI 规划。MVP 应作为“首轮默认展示 / 可见子集”。

#### 建议处理方式

在 `### 建造面板` 后追加：

```md
### MVP 首轮建造卡子集
```

内容写明：

- MVP 首轮建造面板中，玩家必须能看到房屋、农田、水井、树场。
- 房屋可显示推荐，但不得锁定其他三类基础建筑。
- 其他完整分类、搜索、筛选、未解锁折叠仍属于长期 UI，不得因此删除。
- 前30秒不得自动切换 Tab，不得自动选择建筑，不得自动确认建造。

#### 禁止做法

- 不得删除五个一级 Tab。
- 不得删除免费重新放置、首建加速、分类教程和存档恢复规则。
- 不得把当前任务推荐作为第一座建筑前置条件。

---

### 8. `5.3 建筑详情 UI.md`

#### 现有状态

该文件内容明显偏完整游戏，已有：

- 建筑内部小窗预览。
- 双击全屏切入。
- 多分区内部空间。
- 17 种建筑内部交互点。
- 房屋床位面板。
- 农田、水井、树场内部信息。
- 熔炉房、采掘场、太阳灯、农舍、哨塔、研究站、工坊等中后期建筑。
- 通用组件。
- 专业化效率拆解。
- 高度分区反馈。

#### 风险判断

高风险。

该文件与当前 MVP 冻结方向冲突较明显：MVP 不应要求完整建筑内部视角或 17 种建筑分区。第一批不能修改或删除大段内容，只能加“当前 MVP 启用子集 / 完整内部视图延后”。

#### 建议处理方式

在文件开头 `# 建筑详情UI（通用规范）` 后添加：

```md
## MVP 建筑详情启用子集
```

内容写明：

- MVP 建筑详情 UI 只要求显示建筑名称、状态、施工 / 运行 / 暂停、工位、输入、输出、等待原因、可用 / 不可用原因。
- 建筑内部小窗、全屏切入、多分区交互、手动操作点、17 种建筑完整内部视图不作为 MVP 必做。
- 房屋床位面板可作为 MVP 房屋反馈的低成本部分，但不得要求完整室内模拟。
- 农田、水井、树场只显示最小运行状态，不要求手动交互点。

#### 禁止做法

- 不得删除现有内部视图长期规划。
- 不得把完整内部视图写成 MVP 必做。
- 不得把熔炉、采掘场、太阳灯、工坊等建筑详情写入 MVP。

---

## 冲突与风险汇总

待 Codex / 用户审核后回写。

### 风险 1：MVP 子集与全量系统混淆

表现：

- `4.2`、`4.1`、`5.1`、`5.2`、`5.3` 都是全量设计文件。
- 如果只追加“MVP 必做”，容易被误读为要删除全量设计，或把全量设计全部拉进 MVP。

建议：

- 所有第一批回写都使用“启用子集”“MVP 主循环子集”“MVP 首屏子集”等术语。
- 避免使用“替换”“重写”“删除原规则”等措辞。

### 风险 2：NPC 自动补位与现有自动分配边界冲突

表现：

- MVP 闭环想要空闲 NPC 自动补位到施工 / 生产。
- `3.3` 现有规则明确系统不得自动替玩家确认首次分配、自动换岗或自动解除原岗位。

建议：

- 第一批中把自动补位限定为“施工参与 / 空闲杂活 / MVP 低成本劳动力响应”。
- 不得改写正式岗位分配必须玩家确认的原则。
- 真正自动岗位分配另开专题裁决。

### 风险 3：建筑内部视图与 MVP 裁剪冲突

表现：

- `5.3` 已包含完整内部视图和 17 种建筑多分区。
- MVP 综合裁决建议完整内部视图不进入第一批。

建议：

- 第一批只新增“MVP 建筑详情启用子集”。
- 将完整内部视图明确为长期规划或 MVP 后第一轮候选，不作为 MVP 必做。

### 风险 4：资源清单稳定 ID 不可因 MVP 裁剪被删除

表现：

- `4.1` 已维护 16 种资源和稳定 ID。
- MVP 只需要 5 个核心状态 / 资源。

建议：

- 增加 MVP 主循环资源子集说明。
- 不删除全量资源表，不改稳定 ID。

### 风险 5：建造 UI 完整结构不可被四张卡覆盖

表现：

- `5.2` 已有五个一级 Tab、搜索、筛选、任务推荐、重新放置、首建加速等规则。
- MVP 建议只展示四张基础建筑卡。

建议：

- 写成“MVP 首轮建造卡子集”。
- 不修改完整建造 UI 分类结构。

---

## 拆批建议

待 Codex / 用户审核后回写。

### 第一小批：MVP 总体验与基础闭环

建议先处理：

1. `00.27 前30秒与首次城市响应规则_v1.md`
2. `4.2 生产链路总览.md`
3. `4.1 全资源清单.md`

目标：先冻结 MVP 首轮体验和基础资源闭环，避免后续建筑 / UI / NPC 回写没有总依据。

### 第二小批：建筑、NPC、UI 启用子集

建议再处理：

1. `2.1 建筑通用规则.md`
2. `3.3 工作分配逻辑.md`
3. `5.1 全局界面.md`
4. `5.2 建造系统 UI.md`
5. `5.3 建筑详情 UI.md`

目标：把“完整系统文档中的 MVP 启用子集”写清楚。

### 第三小批：四个基础建筑详情

建议最后处理：

1. `房屋.md`
2. `农田.md`
3. `水井.md`
4. `树场.md`

目标：逐个确认四个基础建筑的正文结构后，再写入 MVP 职责和停止线。

---

## 后续建议

待 Codex / 用户审核后回写。

下一步不建议直接启动正式回写。

建议先继续预读四个基础建筑详情文件：

1. `文明块/02 - 建筑系统/单个建筑详情/房屋.md`
2. `文明块/02 - 建筑系统/单个建筑详情/农田.md`
3. `文明块/02 - 建筑系统/单个建筑详情/水井.md`
4. `文明块/02 - 建筑系统/单个建筑详情/树场.md`

并生成一个单独的“四个基础建筑 MVP 职责预读与停止线”暂存文件。

原因：

- 四个建筑是 MVP 闭环核心。
- 每个建筑现有正文可能已经包含中后期机制。
- 需要避免把燃料、工具、复杂室内、生态、多作物、水质、地下联动等内容误写成 MVP 必做。

---

## 禁止直接生效事项

待 Codex / 用户审核后回写。

以下事项不得因为本建议文件而直接生效：

1. 不得直接修改 `文明块/` 正式目录。
2. 不得直接修改 `00.3`、`00.4`。
3. 不得修改 Runtime / C# / Unity / 测试代码。
4. 不得新增正式目录文件。
5. 不得创建 PR，除非用户明确要求。
6. 不得把本预读文件称为正式规则。
7. 不得把“文件适合小改”理解为“现在可以直接改”。
8. 不得删除全量设计内容来迎合 MVP 裁剪。
9. 不得把中后期系统提前写入 MVP。
10. 不得在未预读四个基础建筑详情文件前正式回写四个建筑详情。
