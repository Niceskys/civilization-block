# 2026-07-05 第一批MVP冻结后00.3与00.4治理更新方案建议

> 状态：治理文件更新方案。  
> 本文件位于 `ChatGPT_修改暂存区/`，不是正式规则源。  
> 本文件只规划后续如何更新 `00.3 正式源文件回写路线图.md` 与 `00.4 全库Markdown审计索引.md`。  
> 本文件不直接修改 `00.3`、`00.4`、`00.5`、`6.1`，不修改 Runtime / C# / Unity 工程，不创建 PR。

---

## 本方案结论

第一批 MVP 冻结正式回写已经完成三小批并通过 Codex 审查。

下一步可以进入治理文件更新，但必须分成两个安全步骤：

1. 先更新 `00.3 正式源文件回写路线图.md`，记录第一批 MVP 冻结正式回写的完成状态和 12 个正式回写文件。
2. 再更新 `00.4 全库Markdown审计索引.md`，但必须先由审计脚本或等价全库扫描重新生成 Markdown 文件总数、等级分布和新增暂存区文件索引，不得手写猜测。

推荐顺序：

```md
00.3 更新方案确认 → 00.3 正式回写 → Codex 审查 → 00.4 审计扫描 → 00.4 正式回写 → Codex 审查 → 再决定是否创建 PR
```

---

## 当前依据

### 1. 第一批 MVP 冻结正式回写收口报告

已生成：

```md
ChatGPT_修改暂存区/2026-07-05_第一批MVP冻结正式回写收口报告_建议.md
```

该报告已通过 Codex 内容审查；仅有的 trailing whitespace 问题已经清理。

### 2. 当前 `00.3` 状态

`00.3 正式源文件回写路线图.md` 当前版本为 v3.4，修正日期仍为 2026-07-05。

当前 `00.3` 已记录：

- 00.2 v2.19 治理收口状态；
- RW10-A、RW11-A、RW12-B 已完成；
- RW12-A、RW10-B、RW11-B 部分完成；
- 推荐执行顺序中最后包含“更新 `00.4 全库Markdown审计索引.md`”与“暂停复核，再决定是否继续进入 RW9-C/D/E/F 或合并 PR”。

但 `00.3` 尚未记录本次第一批 MVP 冻结正式回写：

- 第一小批：MVP 总体验与资源闭环；
- 第二小批：建筑 / NPC / UI 启用子集；
- 第三小批：四个基础建筑详情职责与停止线；
- 12 个正式回写文件；
- Codex 三小批审查结论；
- 第一批 MVP 冻结主体完成但仍有未裁决问题。

### 3. 当前 `00.4` 状态

`00.4 全库Markdown审计索引.md` 当前版本为 v3.2。

当前记录：

- 实际 Markdown 文件数：148；
- 等级分布：A101 / B10 / C4 / D33；
- 已纳入 00.26 / 00.27 / 00.28 与第九至十三轮治理材料。

本分支新增了大量 `ChatGPT_修改暂存区/` 文件，因此 `00.4` 若要更新，必须重新扫描全库 Markdown 文件，不能手动沿用 148 或手写猜测新增数量。

---

## `00.3` 建议更新方案

### 更新目标

在 `00.3` 中增加一个新的收口章节，建议标题为：

```md
## RW13-A：第一批 MVP 冻结正式回写
```

或：

```md
## 第一批 MVP 冻结正式回写收口
```

推荐使用 `RW13-A`，原因是当前 `00.4` 版本说明已经提到第九至十三轮治理材料，且 `00.3` 当前已有第十至十二轮内容裁决回写计划。使用 `RW13-A` 可以避免和旧 P0/P1/P2 冲突编号混淆。

### 建议插入位置

建议插入在 `RW12-B：世界观、文明核心与未知区域解释回写` 之后、`推荐执行顺序` 之前。

理由：

- 它是当前最新一批正式回写；
- 不应插入到旧 RW9/RW10/RW11/RW12 小节内部；
- 放在推荐执行顺序前，便于后续更新推荐顺序。

### 建议新增内容

新增章节应包含：

1. 状态：已完成，Codex 审查通过。
2. 目标：将 MVP 第一版范围冻结为“基础城市闭环”，防止任务、商店、地下、工具、科技、联机、模组等长期系统进入 MVP 第一版必做。
3. 三小批回写摘要：
   - 第一小批：MVP 总体验与资源闭环；
   - 第二小批：建筑 / NPC / UI 启用子集；
   - 第三小批：四个基础建筑详情职责与停止线。
4. 已修改 12 个正式文件：
   - `文明块/00 - 项目总纲/00.27 前30秒与首次城市响应规则_v1.md`
   - `文明块/04 - 资源与生产链/4.2 生产链路总览.md`
   - `文明块/04 - 资源与生产链/4.1 全资源清单.md`
   - `文明块/02 - 建筑系统/2.1 建筑通用规则.md`
   - `文明块/03-NPC 系统/3.3 工作分配逻辑.md`
   - `文明块/05-UI 与交互设计/5.1 全局界面.md`
   - `文明块/05-UI 与交互设计/5.2 建造系统 UI.md`
   - `文明块/05-UI 与交互设计/5.3 建筑详情 UI.md`
   - `文明块/02 - 建筑系统/单个建筑详情/房屋.md`
   - `文明块/02 - 建筑系统/单个建筑详情/农田.md`
   - `文明块/02 - 建筑系统/单个建筑详情/水井.md`
   - `文明块/02 - 建筑系统/单个建筑详情/树场.md`
5. 未修改范围：
   - `00.4`；
   - `00.5`；
   - `6.1`；
   - Runtime / C# / Unity / 测试代码；
   - 存档结构；
   - StateHash；
   - 联机协议；
   - PR。
6. 未裁决问题：
   - 水井 10 石头冷启动；
   - 初始资源；
   - 初始 NPC；
   - 初始地皮；
   - 游戏日长度；
   - 生产周期；
   - 消耗周期；
   - 存档格式；
   - StateHash 是否进入 MVP 第一批代码；
   - Runtime 架构正式回写；
   - 地形 / 地基 / 承重 / 通行数据模型正式回写；
   - 是否更新 `00.4`；
   - 是否创建 PR。

### 建议状态更新

`00.3` 文首修正日期建议更新为：

```md
修正日期：2026-07-05（完成第一批 MVP 冻结正式回写收口，新增 RW13-A；12 个正式文件完成 MVP 子集边界回写，三小批均通过 Codex 审查）
```

但不要修改治理冲突统计：

```md
唯一冲突编号总数：44（C001~C044）
活动冲突数量：0
非活动历史记录：44项
```

原因：本批不是冲突编号治理，不应新增 C045 或改变 00.2 v2.19 的冲突统计。

### 推荐执行顺序更新建议

`00.3` 末尾推荐执行顺序应更新为：

1. 保留历史顺序中的 RW9/RW10/RW11/RW12 项。
2. 在 RW11-B 后新增：
   - `RW13-A：第一批 MVP 冻结正式回写`，状态为已完成。
3. 将“更新 `00.4 全库Markdown审计索引.md`”保留为下一步。
4. 保留“暂停复核，再决定是否继续进入 RW9-C/D/E/F 或合并 PR”。

禁止直接写成“创建 PR”或“进入代码实现”。

---

## `00.4` 建议更新方案

### 更新目标

`00.4` 的更新目标不是解释 MVP 规则，而是使全库 Markdown 审计索引与当前分支真实文件一致。

### 必须先做的事

更新 `00.4` 前必须先完成以下检查：

1. 全库 Markdown 文件扫描。
2. 统计总数。
3. 统计等级分布。
4. 检查新增 `ChatGPT_修改暂存区/` 文件是否全部进入索引。
5. 检查路径唯一性。
6. 检查 00.4 是否包含自身。
7. 检查是否有缺失、重复、多余索引项。
8. 检查新增正式回写文件是否仍在索引中，且路径没有变化。

### 不得手写猜测

禁止直接把当前 148 改成一个猜测数字。

原因：本分支已经新增大量 `ChatGPT_修改暂存区/` 文件，具体总数、等级分布和索引顺序必须由扫描结果确认。

### 建议方法

优先使用仓库已有审计脚本或等价脚本，例如：

- `audit_scan.py`
- `validate_index.py`
- `build_master_table.py`

如果脚本不可用，至少应使用等价全库扫描，输出：

- 所有 Markdown 路径；
- 总数；
- 等级分类；
- 与现有 `00.4` 索引的差异；
- 新增文件清单；
- 缺失文件清单；
- 重复项清单。

### `00.4` 建议版本更新

如果扫描通过，`00.4` 可从 v3.2 更新为 v3.3。

版本说明建议为：

```md
| 3.3 | 2026-07-05 | 同步第一批 MVP 冻结正式回写相关暂存区方案、审查与收口文件；按全库扫描结果更新 Markdown 文件总数、等级分布和索引 |
```

### 新增索引项范围

至少应包含本分支新增的以下治理 / 暂存文件，但实际清单以扫描结果为准：

- 第一小批正式回写说明与安全审查报告；
- 第二小批正式回写说明与安全审查报告；
- 第三小批正式回写说明与安全审查报告；
- 第一批 MVP 冻结正式回写收口报告；
- 第一批 MVP 冻结后 `00.3` 与 `00.4` 治理更新方案；
- 本分支此前新增的所有 `ChatGPT_修改暂存区/2026-07-05_*_建议.md` 文件。

### `00.4` 禁止事项

更新 `00.4` 时不得：

- 手写猜测文件数；
- 漏掉 `00.4` 自身；
- 漏掉 `ChatGPT_修改暂存区/` 新增文件；
- 把暂存区建议文件标成 A 级正式规则；
- 改变 00.2 冲突统计；
- 修改正式规则内容；
- 修改 Runtime / C# / Unity / 测试代码；
- 创建 PR。

---

## 推荐执行拆分

### 步骤一：只更新 `00.3`

允许修改：

```md
文明块/00 - 项目总纲/00.3 正式源文件回写路线图.md
```

禁止修改：

- `00.4`；
- `00.5`；
- `6.1`；
- Runtime / C# / Unity / 测试代码；
- PR。

完成后给 Codex 审查。

### 步骤二：准备 `00.4` 扫描结果

先运行审计脚本或等价扫描，输出扫描结果。

此步骤可以先新增暂存区扫描结果文件，不直接改 `00.4`。

建议文件名：

```md
ChatGPT_修改暂存区/2026-07-05_00.4更新前全库Markdown扫描结果_建议.md
```

### 步骤三：正式更新 `00.4`

只有在扫描结果通过后，才更新：

```md
文明块/00 - 项目总纲/00.4 全库Markdown审计索引.md
```

完成后再让 Codex 审查。

### 步骤四：再决定是否创建 PR

只有在 `00.3` 和 `00.4` 均通过审查后，再决定是否创建 PR。

---

## 给 Codex 的审查建议

Codex 审查本方案时，应确认：

1. 本方案是否没有直接修改 `00.3` / `00.4`。
2. `00.3` 更新方案是否准确记录第一批 MVP 冻结正式回写。
3. `00.3` 是否没有建议新增冲突编号或改变 C001~C044 统计。
4. `00.4` 更新方案是否要求先全库扫描，而不是手写猜测文件数。
5. `00.4` 是否明确不得把暂存区建议文件标成 A 级正式规则。
6. 后续执行是否拆成 `00.3`、扫描、`00.4`、PR 裁决四步。
7. 本方案是否没有要求修改 Runtime / C# / Unity / 测试代码。

---

## 禁止事项

后续不得因为本方案而直接：

1. 修改 `00.3`。
2. 修改 `00.4`。
3. 修改 `00.5`。
4. 修改 `6.1`。
5. 修改 Runtime / C# / Unity / 测试代码。
6. 创建 PR。
7. 手写猜测 `00.4` 文件总数。
8. 新增 C045 或改变 00.2 冲突统计。
9. 把暂存区建议文件标成 A 级正式规则。
10. 把未裁决问题写成已定稿规则。
