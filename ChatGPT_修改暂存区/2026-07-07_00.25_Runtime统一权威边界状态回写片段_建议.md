# 2026-07-07 00.25 Runtime统一权威边界状态回写片段建议

> 状态：暂存区建议片段。
> 本文件位于 `ChatGPT_修改暂存区/`，不是正式规则源。
> 本文件只提供后续回写 `00.25 后太阳灯阶段Runtime推进计划_v1.md` 的插入位置建议与正文片段。
> 本文件不修改 `00.25`、`00.5`、`ArchitectureMapping.md`、`00.3`、`00.4`，不修改 Runtime / C# / Unity / 测试代码，不创建 PR。

---

## 1. 建议插入位置

建议插入在 `文明块/00 - 项目总纲/00.25 后太阳灯阶段Runtime推进计划_v1.md` 的 `## 一、当前基线` 内部，优先插入到 `当前验证状态` 小节之后、`当前不建议立即进入` 小节之前。

如果后续本地文件结构已经变化，则建议插入在当前基线 / 当前 Runtime 基线 / 当前已完成事项附近，由 Codex 本地读取完整文件后精确定位。

---

## 2. 建议插入正文

```md
### Runtime 统一权威边界正式回写状态

已完成事项：

- `文明块/00 - 项目总纲/00.5 Runtime架构蓝图_v1.md` 已新增 `Runtime 统一权威边界`。
- `文明块/Runtime/Authority/ArchitectureMapping.md` 已新增 `Runtime 统一权威边界映射`。
- 两个文件已经覆盖以下 Runtime 架构边界：
  - Definition；
  - State；
  - View；
  - Command；
  - Event；
  - Simulation；
  - Save；
  - StateHash；
  - ServerAuthority。

当前结论：

- Runtime 架构边界已经具备正式文档承载。
- `Simulation / RuntimeComposition` 是单机与服务器共用权威入口。
- UI / View 只能读取 Runtime 输出或提交 Command，不得修改权威 State。
- Event 是事实输出，不是修改入口。
- Save / StateHash 只覆盖权威状态边界。
- Remote Client 不推进权威 Simulation，Server 是联机权威。
```

---

## 3. 后续待同步与禁止扩大范围

后续待同步：

- `00.3 正式源文件回写路线图` 尚未同步。
- `00.4 全库Markdown审计索引` 尚未同步。
- UI / 建筑 / 三维占地文档尚未同步 Runtime 边界。
- 是否创建 PR 尚未裁决。

禁止扩大范围：

- 不得把完整昼夜系统写成当前阶段必做。
- 不得把 UI 实现写成当前阶段必做。
- 不得把联机服务器产品化写成当前阶段必做。
- 不得把模组热加载写成当前阶段必做。
- 不得把新 Runtime 系统写成当前阶段必做。
- 不得把新存档结构写成当前阶段必做。
- 不得把新 StateHash 算法写成当前阶段必做。
- 不得把新协议版本写成当前阶段必做。

本片段仅用于后续安全回写 `00.25` 的状态同步，不代表正式规则已经写入 `00.25`。
