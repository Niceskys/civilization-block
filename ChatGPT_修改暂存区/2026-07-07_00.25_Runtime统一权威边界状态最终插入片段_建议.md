# 00.25 Runtime 统一权威边界状态最终插入片段建议

## 建议插入位置

建议插入到 `文明块/00 - 项目总纲/00.25 后太阳灯阶段Runtime推进计划_v1.md` 的 `## 一、当前基线` 内部，优先放在 `当前验证状态` 后、`当前不建议立即进入` 前。

## 最终插入正文

### Runtime 统一权威边界正式回写状态

截至本片段整理时，Runtime 统一权威边界已经完成以下正式源回写：

- `00.5 Runtime架构蓝图_v1.md` 已新增 `Runtime 统一权威边界`。
- `Runtime/Authority/ArchitectureMapping.md` 已新增 `Runtime 统一权威边界映射`。
- 两处内容共同覆盖 Definition / State / View / Command / Event / Simulation / Save / StateHash / ServerAuthority。
- `Simulation / RuntimeComposition` 是单机与服务器共用权威入口。
- UI / View 只能读取 Runtime 输出或提交 Command，不得修改权威 State。
- Event 是事实输出，不是修改入口。
- Save / StateHash 只覆盖权威状态边界。
- Remote Client 不推进权威 Simulation，Server 是联机权威。

后续待同步：

- `00.3 正式源文件回写路线图` 尚未同步。
- `00.4 全库Markdown审计索引` 尚未同步。
- UI / 建筑 / 三维占地文档尚未同步 Runtime 边界。
- 是否创建 PR 尚未裁决。

当前停止线 / 禁止扩大范围：

当前阶段不得进入以下范围：

- 完整昼夜系统
- UI 实现
- 联机服务器产品化
- 模组热加载
- 新 Runtime 系统
- 新存档结构
- 新 StateHash 算法
- 新协议版本
- 正式任务 / 商店 / 地下 / 科技 / 防御系统
