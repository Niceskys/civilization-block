# Runtime 架构正式回写后 00.3 与 00.4 治理同步方案建议

## 一、方案性质

本文仅作为 `00.3 正式源文件回写路线图` 与 `00.4 全库Markdown审计索引` 的后续治理同步建议，不直接修改正式规则源，不替代正式源文件结论，不创建 PR。

当前已经确认：GitHub 连接器读取 `00.25 后太阳灯阶段Runtime推进计划_v1.md` 长文件时存在截断风险，因此不得继续通过 GitHub 连接器直接修改该长文件。`00.25` 的正式插入应等待本地完整文件读取、受控编辑与校验后再执行。

## 二、已完成提交记录

本轮 Runtime 统一权威边界正式回写链路已经形成以下提交记录：

- `35fac6f`：Runtime 架构回写方案暂存文件。
- `c52efdc`：`00.5` 正式回写 Runtime 统一权威边界。
- `809a651`：`ArchitectureMapping.md` 映射 Runtime 权威边界。
- `47b2bd4`：`00.25` 插入片段初版建议。
- `4db7be1`：`00.25` 最终插入片段建议。

## 三、`00.3 正式源文件回写路线图` 后续同步建议

后续同步 `00.3` 时，只建议记录治理状态，不扩大 Runtime 功能范围。

建议在 `00.3` 中补充以下信息：

1. 记录 Runtime 架构正式回写第一批已经完成。
2. 标记已完成正式回写文件：
   - `00.5 Runtime架构蓝图_v1.md`
   - `Runtime/Authority/ArchitectureMapping.md`
3. 标记待回写或待同步文件：
   - `00.25 后太阳灯阶段Runtime推进计划_v1.md`
   - `00.3 正式源文件回写路线图.md`
   - `00.4 全库Markdown审计索引.md`
   - UI / 建筑 / 三维占地相关文件
4. 明确 `00.25` 因长文件截断风险，正式插入暂缓；当前已有最终插入片段建议，可在本地完整读取、编辑、校验后再回写。
5. 明确本轮治理同步不代表新增 Runtime 系统，不代表调整存档结构、StateHash 算法或协议版本。

## 四、`00.4 全库Markdown审计索引` 后续同步建议

后续同步 `00.4` 时，应以实际文件落地结果为准，不应手工猜测全库索引。

建议遵守以下原则：

1. 等待实际文件落地后再更新索引。
2. 不手工猜测全库 Markdown 数量、路径、状态或索引编号。
3. 应通过本地完整扫描或受控 Markdown 索引脚本更新。
4. 暂存区建议文件可以被记录为暂存区文件，但不得误称为正式规则源。
5. `00.4` 更新前应先确认以下文件的真实状态：
   - `00.25 后太阳灯阶段Runtime推进计划_v1.md` 是否已经完成正式插入。
   - `00.3 正式源文件回写路线图.md` 是否已经记录本轮 Runtime 架构正式回写状态。
   - UI / 建筑 / 三维占地相关文件是否已经完成 Runtime 边界同步。
6. 若只新增或更新暂存区建议文件，不应在 `00.4` 中提前写成正式规则源已经完成。

## 五、当前停止线

当前阶段仍不得进入以下范围：

- 完整昼夜系统
- UI 实现
- 联机服务器产品化
- 模组热加载
- 新 Runtime 系统
- 新存档结构
- 新 StateHash 算法
- 新协议版本
- 正式任务 / 商店 / 地下 / 科技 / 防御系统

## 六、建议执行顺序

1. 保留本文件作为治理同步方案暂存记录。
2. 等待本地工作区完整读取并编辑 `00.25`，插入最终片段建议内容。
3. 在 `00.25` 实际落地后，再同步 `00.3` 的路线图状态。
4. 在实际文件变更全部完成后，通过本地完整扫描或受控 Markdown 索引脚本更新 `00.4`。
5. 在任何正式源回写前，继续保持单文件、小范围、可审查提交。
