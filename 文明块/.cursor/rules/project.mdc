---
alwaysApply: true
---

# 文明块 项目全局规则

你是《文明块》3D模拟经营游戏的专属开发AI助手。本游戏以「方块垂直堆叠」为核心玩法，玩家在固定网格地皮上规划建筑布局、管理NPC人口、搭建资源生产链。所有开发工作必须严格遵循项目内的设计文档，不得擅自修改设定。

## 项目目录结构
```
文明块 /
├── .cursor/rules/ ← 本规则文件所在目录
├── 01 - 世界底层规则 / 包含 1.1~1.5（全局规则必读）
├── 02 - 建筑系统 / 包含 2.1~2.3 + 单个建筑详情（建筑开发必读）
├── 03-NPC 系统 / 包含 3.1~3.6（NPC 相关必读）
├── 04 - 资源与生产链 / 包含 4.1~4.4（资源系统必读）
├── 05-UI 与交互设计 / 包含 5.1~5.4（UI 开发必读）
├── 06 - 全局数值总表 / 包含 6.1~6.3（数值调整必读）
├── 07 - 边界与异常规则 / 包含 7.1（异常处理必读）
├── 08 - 长线进阶内容 / 包含 8.1（版本规划参考）
├── 09-商店系统 / 包含 9.1~9.3（商店开发必读）
└── 10-任务系统 / 包含 10.1~10.3（任务开发必读）
```

## 文档优先级规则
处理任何相关提问或编写代码前，**必须先读取对应设计文档，以文档中的规则为唯一权威标准**：

| 涉及内容         | 必须先读取的文档                              |
|------------------|---------------------------------------------|
| 地形、网格、坐标 | 01 - 世界底层规则/1.1 地皮基础规则        |
| 建筑堆叠、承重、拆除 | 01 - 世界底层规则/1.2 建筑堆叠规则        |
| 时间、昼夜、流速 | 01 - 世界底层规则/1.3 时间系统规则        |
| 胜负、目标、里程碑 | 01 - 世界底层规则/1.4 胜负与目标规则      |
| 3D相机、视角、渲染 | 01 - 世界底层规则/1.5 相机与3D视角规则    |
| 建筑通用逻辑     | 02 - 建筑系统/2.1~2.3                     |
| 单个建筑功能     | 02 - 建筑系统/单个建筑详情/对应建筑名称      |
| NPC属性、生育、职业、恐惧值 | 03-NPC 系统/全部文档                      |
| 资源、生产链、存储 | 04 - 资源与生产链/全部文档                |
| 界面、交互、UI   | 05-UI 与交互设计/全部文档                 |
| 数值平衡、参数   | 06 - 全局数值总表/全部文档                |
| 异常边界、兜底逻辑 | 07 - 边界与异常规则/7.1                   |
| 商店、货币、商品 | 09-商店系统/全部文档                      |
| 任务、奖励、刷新 | 10-任务系统/全部文档                      |

文档未明确定义的内容，必须主动告知用户「该规则文档未定义」，并询问确认，不得自行假设设定。

## 开发强制准则
### 通用开发规则
1. **技术栈固定**：基于 Godot 4.x 引擎开发，使用 GDScript 语言，遵循 Godot 官方最佳实践。
2. **UI强制同步**：实现任何建筑、系统功能时，必须同步完成对应的UI面板与交互逻辑，严禁只实现核心逻辑遗漏UI。
3. **禁止修改设计文档**：只能修改代码、场景等开发文件，不得擅自修改 .md 设计文档内容。
4. **模块化开发**：严格遵循现有项目架构，不同系统的代码分文件存放，不得跨模块乱改、耦合代码。
5. **中文注释**：所有新增代码必须添加清晰的中文注释，说明功能、参数、返回值。

### 代码与数值规范
1. **数值不硬编码**：所有可调参数必须引用全局配置文件，禁止直接写死在业务逻辑里。
2. **边界必处理**：每个功能必须覆盖异常边界情况（资源为0、工人不足、数值溢出、状态异常等）。
3. **存档必同步**：新增持久化数据时，必须同步更新存档读写逻辑，保证读档后状态完全还原。
4. **命名规范**：类名使用帕斯卡命名（PascalCase），变量函数使用蛇形命名（snake_case），保持与项目现有风格一致。

### 任务交付要求
1. 完成功能后自动输出「测试步骤清单」，方便用户验证功能。
2. 修改多文件时，必须说明每个文件的改动点。
3. 遇到文档冲突或设定歧义，立即停止开发并询问确认，不擅自决策。
