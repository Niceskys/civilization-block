---
tags:
  - 项目总纲
  - DeepSeek
  - 任务包
  - 测试
version: 1.0
status: 可执行
author: 核心框架设计
---

# 00.13 DeepSeek低风险任务包 2026-06-18
> 使用前必须先让 DeepSeek 阅读 `00.9 DeepSeek代码协作规范_v1.md`。
> 三个任务必须按顺序、一次只执行一个；每个任务验收通过后才能开始下一个。

---

## DS-20260618-01：补核心边界测试

**执行状态**：已完成并由 Codex 复核。新增3项测试，Runtime 总测试数由6项增至9项，构建0错误、0警告。

**目标**：只扩充现有测试，不修改生产代码。

**唯一允许修改**：

- `文明块/Runtime.Tests/Program.cs`

**禁止修改**：

- `文明块/Runtime/Authority/` 下全部文件；
- 项目文件、玩法文档、工具脚本；
- 任何依赖或包配置。

**任务步骤**：

1. 为 `StableId` 增加空字符串、含空格、缺少命名空间的拒绝测试。
2. 为 `ModRegistry` 增加重复 `ModId` 被拒绝且加载数量不变的测试。
3. 为 `StateDiagnostics` 增加空 `GameState` 检查不抛异常且能生成状态哈希的测试。
4. 沿用当前 `Run` 和断言方法，不引入测试框架。

**验收**：

```powershell
powershell -ExecutionPolicy Bypass -File tools\test-runtime.ps1
```

要求构建 0 错误、0 警告，原 6 项与新增测试全部通过。

**停止条件**：测试暴露生产代码问题时立即停止并报告，不得修改生产代码迁就测试。

---

## DS-20260618-02：补注册表与事件流测试

**执行状态**：已完成并由 Codex 复核。新增4项测试，Runtime 总测试数由9项增至13项，构建0错误、0警告。

**前置条件**：DS-20260618-01 已通过。

**唯一允许修改**：

- `文明块/Runtime.Tests/Program.cs`

**任务步骤**：

1. 验证 `DefinitionRegistry.RegisterBuilding(null)` 抛出参数异常。
2. 验证非法建筑 Definition ID 被拒绝。
3. 验证合法建筑定义注册后可由 `TryGetBuilding` 取回。
4. 验证 `EventStream.Publish` 同时把事件写入状态并触发一次订阅回调。
5. 不测试或修改事件网络同步、线程安全、持久化。

**验收**：同 DS-20260618-01。

**停止条件**：不得为了测试方便放宽 ID 校验、改变事件写入顺序或新增公开 API。

---

## DS-20260618-03：改善索引校验失败提示

**执行状态**：已完成并由 Codex 复核。正常索引保持PASS；索引缺失与表头损坏均能明确报错并返回失败状态。

**前置条件**：前两个任务已通过，且单独提交修改结果。

**唯一允许修改**：

- `文明块/00 - 项目总纲/validate_index.py`

**禁止修改**：

- `generate_index.py`；
- `00.4 全库Markdown审计索引.md`；
- 其他 Markdown；
- 当前统计和通过条件。

**任务步骤**：

1. 当索引文件不存在时，输出明确的文件路径和“先运行 generate_index.py”的建议。
2. 当索引表头无法识别时，明确报错，不得静默得到空索引。
3. 给核心函数添加简短说明，禁止大规模重排或重构。
4. 保持当前正常仓库的输出字段和 PASS 结果兼容。

**验收**：

```powershell
python "文明块\00 - 项目总纲\validate_index.py"
```

要求 `ACTUAL_MD_FILES` 与 `INDEXED_PATHS` 相等，缺失、多余、重复均为 0，最终结果为 PASS。

**停止条件**：如果必须改索引格式、统计规则或生成器才能完成，立即停止并报告。

---

## 统一回报格式

DeepSeek 每个任务完成后只回报：

1. 修改了哪些文件；
2. 增加或改变了什么；
3. 实际运行的验收命令与结果；
4. 是否触发停止条件；
5. `git diff --stat` 摘要。

不得附带未实施的架构方案，不得继续执行下一个任务。
