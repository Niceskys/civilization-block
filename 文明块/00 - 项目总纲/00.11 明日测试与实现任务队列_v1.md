---
tags:
  - 项目总纲
  - 测试计划
  - DeepSeek任务
  - 执行队列
version: 1.0
status: 补充中
author: 核心框架设计
---

# 00.11 明日测试与实现任务队列 v1
> 建立日期：2026-06-17
> 用途：在额度恢复后，按低风险顺序继续推进测试、文档治理与DeepSeek可执行任务。
> 本文件是执行队列，不覆盖A/B级玩法规则。

---

## 一、明天优先顺序

1. 先跑现有Runtime测试，确认今天文档治理后的仓库状态仍然干净。
2. 再处理文档治理类问题，优先关闭不需要玩法裁决的问题。
3. 最后再把低风险、边界清楚的小任务交给DeepSeek。

不建议明天一开始就做联机、模组沙箱、存档迁移或大型UI框架，因为这些需要更长测试链。

---

## 二、明天第一轮验证

必跑：

- `tools/test-runtime.ps1`
- `文明块/00 - 项目总纲/validate_index.py`

检查目标：

- Runtime构建0错误；
- Runtime smoke tests全部通过；
- Markdown索引无缺失、无多余、无重复；
- 建筑详情文件缺失`status`数量保持为0；
- 测试后清理`.appdata/`、`.dotnet_home/`、`.nuget_packages/`、`bin/`、`obj/`。

---

## 三、Codex优先处理任务

### T-CODEX-01：C018引用核查

状态：已完成，结果见 `00.12 C018文档引用核查报告_v1.md`。

目标：找出文档中不存在的文件名、章节名或已移动路径引用。

范围：

- 只读全库Markdown；
- 输出问题清单；
- 不直接批量替换；
- 需要人工确认后再改A/B级文件。

验收：

- 生成一份引用问题表；
- 标记每条问题属于“路径已归档”“文件名变化”“章节不存在”“误报”；
- 不制造新规则。

### T-CODEX-02：DeepSeek任务包整理

状态：已完成，结果见 `00.13 DeepSeek低风险任务包_2026-06-18.md`。

目标：从现有任务中拆出DeepSeek可做的小任务。

范围：

- 每个任务只允许改1到3个文件；
- 每个任务必须有明确验收；
- 不允许DeepSeek改`GameState.cs`、`Simulation.cs`、`CommandBus.cs`、`SaveSystem.cs`、`StableId.cs`、`ModRegistry.cs`，除非任务书单独批准。

验收：

- 得到3到5个可直接投喂DeepSeek的小任务；
- 每个任务包含：允许文件、禁止文件、步骤、验收命令、失败时停止条件。

---

## 四、DeepSeek可执行候选任务

### DS-01：补Runtime测试用例

允许：

- `文明块/Runtime.Tests/Program.cs`

禁止：

- 修改Runtime生产代码；
- 修改玩法文档；
- 新增依赖。

任务：

- 为`StableId`补非法字符、空字段、命名空间缺失测试；
- 为`ModRegistry`补重复mod id测试；
- 为`StateDiagnostics`补空状态不报错测试。

验收：

- `tools/test-runtime.ps1`通过；
- 测试失败时不得改生产代码来迁就测试。

### DS-02：补文档工具注释与错误输出

允许：

- `文明块/00 - 项目总纲/generate_index.py`
- `文明块/00 - 项目总纲/validate_index.py`

禁止：

- 改索引格式；
- 改规则统计；
- 改任何Markdown正文。

任务：

- 让错误输出更清楚；
- 保持当前PASS结果不变。

验收：

- `validate_index.py`通过；
- 重新生成索引后文件数不异常变化。

### DS-03：补DeepSeek任务模板

允许：

- `文明块/00 - 项目总纲/00.9 DeepSeek代码协作规范_v1.md`

禁止：

- 修改架构规则；
- 修改禁止事项；
- 增加新技术栈。

任务：

- 在文末追加一个“可复制任务书模板”；
- 模板包含允许文件、禁止文件、验收命令、停止条件。

验收：

- 模板可直接复制给Claude Code接入DeepSeek使用；
- 不改变既有规范含义。

### DS-04：补太阳灯农田光照边界测试

状态：已完成，提交见`416cdc4 补农田光照边界测试`。

任务声明：

- 指定模型：由核心设计者在OpenCode界面选择并确认；
- 模型用途：仅补Runtime测试，不修改生产代码；
- 运行模式：受限执行；
- 推理强度：中；
- 任务编号：DS-20260627-004；
- 风险等级：低。

必读：

- `文明块/00 - 项目总纲/00.9 DeepSeek代码协作规范_v1.md`
- `文明块/00 - 项目总纲/00.5 Runtime架构蓝图_v1.md`
- `文明块/00 - 项目总纲/00.24 三维占地与房间模块迁移设计_v1.md`
- `文明块/Runtime.Tests/Program.cs`
- `文明块/Runtime/Authority/ContinuousProductionSystem.cs`
- `文明块/Runtime/Authority/AgriculturalLightRules.cs`

允许：

- `文明块/Runtime.Tests/Program.cs`

禁止：

- 修改`文明块/Runtime/Authority/`下任何生产代码；
- 修改存档版本、协议版本、Tick顺序或Runtime装配；
- 修改农田、太阳灯、树场、昼夜、燃料或UI规则文档；
- 新增依赖、脚本、工具或测试框架；
- 为了测试通过而删除、放松或跳过既有断言。

任务：

1. 只在现有Runtime测试风格中补小型测试；
2. 覆盖`paused_no_light`状态的存档往返，确认读档后该状态仍为已知且不破坏连续生产状态；
3. 覆盖缺光暂停后恢复光照的边界，确认恢复后只从原有进度继续推进，不补算缺光期间产出；
4. 如果现有测试已经完整覆盖某项，不重复造新辅助框架，只补缺口或在回执中说明无需新增。

验收：

- `powershell -ExecutionPolicy Bypass -File tools\test-runtime.ps1`通过；
- 若测试失败且原因需要修改生产代码，立即停止并输出`BLOCKED`；
- `git diff --name-status`只允许出现`文明块/Runtime.Tests/Program.cs`；
- 回执必须包含真实命令、退出码和关键输出。

---

## 五、暂缓任务

暂缓到测试链更完整后再做：

- 联机服务器；
- 模组热加载；
- 模组脚本沙箱；
- 存档迁移；
- UI完整框架；
- 经济数值大平衡；
- 自动化大批量文档改写。

原因：这些任务一旦做错，bug难以定位；应等Runtime测试、引用核查、任务模板都稳定后再进入。

---

## 六、当前治理状态快照

- 活动问题：0项；
- P0：0项；
- P1：0项；
- P2：0项；
- P3：0项；
- DG：0项；
- Markdown索引：139个文件，已通过校验；
- C042：已关闭；
- C020：已关闭，数值权威与受控摘要校验已建立；
- C025：已关闭，堆叠规则权威边界已建立；
- C004：已关闭，高层暴露分区替代无限效率惩罚；
- C017：已关闭，季节委托永久保留且主题装饰不绝版；
- C026：已关闭，性格改为无通用负效率的情境专长；
- C015/C037：已联合关闭，开放沙盒目标与文明印记边界已建立；
- C001-C044全部进入非活动历史；
- Runtime统一会话切片：已完成Local/Server/Remote边界、授权、隔离快照和事件游标同步；
- 模组兼容握手：已完成协议版本、游戏版本、模组ID、版本、校验和、加载顺序与依赖声明的逐项校验；握手通过前禁止同步与命令；
- StateHash修复：已完成客户端漂移检测、权威哈希校验、单次快照重取与专用同步异常；
- 最小断线重连：已完成重新握手、事件游标续接、断线状态补齐与命令序列连续性；不缓存离线命令；
- 视觉与交互基准：已归档4张参考图并锁定默认城市视角、按需建筑剖面、三条文明路线和防御占比边界；
- 三维占地迁移：Phase A至C已完成；旧存档升级至1.2，Plot具备默认`1x1`边界，实例与施工任务保存不可反推的放置快照；`BuildingSystem`、读档和诊断统一使用确定性旋转、边界、重叠与施工预占校验；多格`Solid`已开放，附着与连接占用仍关闭；现行18类正式建筑数值未批量修改；
- 联机空间契约：建造命令、事件与拒绝结果已携带新坐标和稳定错误码，远端保持错误码；会话协议版本升级至2；
- Phase D承重迁移：D1纯`StructuralSupport`与D2a权威接入已完成；`BuildingSystem`和`StateDiagnostics`统一使用50%最低底面接触、1000倍整数载荷、按接触格共同支撑和稳定ID余数分配；施工任务有重量但完工前不承重；旧按层汇总算法已删除；
- 联机结构契约：无支撑、接触不足、容量超限、状态损坏、规模超限和算术溢出均有稳定错误码并由远端保留；会话协议升级至3；
- 结构事故难度契约：简单/普通/困难/极难在1倍速下分别宽限60/30/15/5现实分钟，崩塌间隔10/5/3/1现实秒；暂停停止、2倍速加快；自定义可选0至120分钟、1至10秒或“停用但不自动摧毁”；
- 权威时间：`1 Tick = 1游戏秒`，1倍速72 Tick/现实秒，2倍速144 Tick/现实秒，暂停0 Tick；退出不补算；难度写入存档；
- D2b结构事故状态机：拆除命令、失稳截止Tick、补建恢复、最高层优先确定性崩塌及“仅停用”均已接入；失稳建筑保留占地但不承重；远端拆除、大步长Tick和事故存档往返已有专项测试；
- 拆除与运行状态契约：新施工保存实际支付成本；正常拆除返还75%并报告容量溢出，坍塌零返还；工人、生产、物流共用统一可运行判定；
- 最小NPC岗位权威层：NPC、岗位、建筑工位数量进入存档；支持稳定槽位、命令分配/解除、原子换岗、NPC或建筑失效即时清理及远端服务器裁决；职业适配、班次和行为尚未实现；存档升级1.6，协议升级7；
- 最小生产批次与局部库存：已完成配方注册、单批/连续生产、原子输入锁、岗位工作量、暂停恢复、取消解锁、单批产物缓冲、整批转移、拆除限制、事故回收及远端服务器裁决；
- 正式管道连接设施：已完成施工、取消、完工、配置、拆除与端点摧毁生命周期；完工路线固定下层到上层及单一资源，每根管道最多一个自动批量任务，临时失稳/停用可暂停并恢复；存档升级1.9，协议升级10；
- Runtime统一装配与定义封存：已建立单机/服务器共用的九系统固定装配入口；定义注册改为快照、重复ID拒绝、启动封存，正式管道由核心内容模块按6.1注册；其余普通建筑仍等待Phase E逐栋迁移；
- 正式资源目录与跨引用校验：已注册4.1全部16种资源及稳定ID；正式定义封存时验证建筑/管道成本、配方输入输出和配方所属建筑，未知引用立即拒绝；
- 初始建筑内容迁移：房屋、农田、水井、树场已按6.1注册稳定ID、成本、施工/首建加速、工位、耐久和承重，并逐栋裁决标准占地、派生重量及20单位缓存；树场应急炭化配方已接入；
- 基础持续生产：已按整数进度结算农田、水井、树场日产量；农田预付1水/运行日，暂停保存进度与灌溉覆盖；满仓保留单单位待转移并阻止拆除；存档升级2.0，协议升级11；
- 房屋床位与入住权威层：房屋Lv1提供2床、每级增加1床；成年且存活、未永久离开的NPC按创建序入住，婴儿不占床，昏迷不失去住房；有效手动床位优先保留，房屋失效后即时释放并确定性迁居，剩余成年NPC进入无家可归集合；状态、命令与事件进入存档和联机协议，室内动画仍只作为后续表现层；存档升级2.1，协议升级12；
- NPC基础生存需求：按游戏日集中结算，成年人每日食物和水各0.5，婴儿食物0.25、水0.5；使用四分之一单位整数余数结转，不使用浮点；只消费全局未锁定库存，保存需求、实耗、缺口与连续短缺天数；大步长Tick逐日结算，旧存档从读档后的下一日开始；暂不因短缺致死或降低效率；存档升级2.2，协议升级13；
- NPC权威生命周期：婴儿精确在2.5游戏日成长；成年寿命按世界种子与NPC稳定ID确定性生成20至40游戏日；阶段年龄、寿命、成长Tick和死亡Tick进入存档与StateHash；大步长可连续处理成长和死亡，事件按真实Tick排序；死亡立即释放住房与岗位，生存结算按当日边界还原阶段和存活状态；旧NPC从读档时刻以当前阶段年龄0开始，不追溯衰老；体质、疗愈所、科技和传奇特质修正尚未接入；存档升级2.3，协议升级14；
- 第二阶段生产闭环：采掘场与熔炉房已按6.1逐栋注册；采掘场每工人每日持续产出2铁矿石与1石材副产物，熔炉房铁/铜配方均将燃料纳入批次原子输入锁；副产物独立保存整数进度与待转移量，满仓阻止继续生产和普通拆除；存档升级2.4，协议升级15；
- 仓库与共享容量：仓库已按6.1与4.3注册，完工后为2000基础全局共享容量增加200；分类上限与共享上限同时裁决，正常拆除保护现有资源和入库预约，事故超容不删除资源；拆除退款、生产与物流统一使用共享容量；存档升级2.5，协议升级16；
- 垃圾处理场模式：A销毁、B确定性加权回收、C肥料和D燃料已作为互斥批次配方接入；B结果由世界种子与批次ID决定，D使用`10垃圾 -> 3燃料`保持0.3比例；模式C染料副产等待正式资源ID；协议升级17，存档保持2.5；
- 垃圾产生与阈值状态：运行且有有效工人的建筑每日产生1垃圾，每名存活NPC每日产生0.5垃圾并用半单位整数余数结转，NPC死亡立即产生1垃圾；500分类上限与共享容量共同裁决溢出；200/400阈值累计满意度惩罚并保存疾病概率增量，清零与次日恢复时点已确定；存档升级2.6，协议升级18；
- 垃圾负面效果接入：NPC基础满意度与垃圾环境惩罚分离；批次和持续生产均按万分比整数余数应用全员效率惩罚；超过400垃圾时按权威种子、日Tick和NPC稳定ID执行确定性疾病暴露判定，暂不虚构病程与治疗数值；存档升级2.7，协议升级19；
- 肥料农业闭环：新增施肥权威命令，每份肥料锁定施用时全部工人的一个基础日产周期并提供50%额外食物；效果不可叠加，暂停、换人、换速和Tick切分不会改变总奖励；状态支持存档、诊断与服务器同步；存档升级2.8，协议升级20；
- 太阳灯与农田光照裁决：核心太阳灯已注册为`building:core:sunlamp`；农田持续生产在有工人后、消耗灌溉水前执行权威光照检查；被上方建筑遮挡且无有效太阳灯完整覆盖时进入`paused_no_light`，不产出、不吞水、不推进进度；太阳灯覆盖恢复生产；当前只接入农田，不制作光照渲染或昼夜系统；
- 太阳灯光照边界测试：已补`paused_no_light`存档往返和缺光恢复不补算暂停期间产出的Runtime测试；当前仍未接入树场光照、昼夜系统、太阳灯燃料消耗、UI提示或光照渲染；
- Runtime测试：202项全部通过，0警告、0错误；测试脚本会在编译或测试失败时可靠返回失败；
- 下一Runtime优先项：按`00.25 后太阳灯阶段Runtime推进计划_v1.md`先做提交链总审查、文档状态回写和光照诊断裁决；暂不进入完整昼夜系统、太阳灯燃料消耗、树场光照、UI渲染或更大范围建筑光照门控。
